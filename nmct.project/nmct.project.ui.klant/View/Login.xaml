<UserControl x:Class="nmct.project.ui.klant.View.Login"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:nmct.project.ui.klant.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400">
    <UserControl.DataContext>
        <vm:LoginVM/>
    </UserControl.DataContext>
    <Grid Width="400" Height="200">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="45" />
            <RowDefinition Height="45" />
        </Grid.RowDefinitions>
        <Button Content="Aanmelden" Grid.Row="1" Style="{StaticResource button_Style}" Command="{Binding LoginCommand}"/>
        <Button Content="Registreren" Grid.Column="1" Grid.Row="1" Style="{StaticResource button_Style}" Command="{Binding RegisterCommand}"/>
        <TextBlock Text="{Binding Error}" Grid.ColumnSpan="2" Grid.Row="2"/>
    </Grid>
</UserControl>
